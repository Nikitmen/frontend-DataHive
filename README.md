##  Технологический стек

| Технология | Назначение | Версия |
|------------|------------|---------|
| **React** | Библиотека для построения UI | 18+ |
| **Vite** | Сборщик и dev-сервер | 5.x |
| **Ant Design** | UI-компоненты и дизайн-система | 5.x |
| **React Router** | Навигация и маршрутизация | 6.x |
| **Docker** | Контейнеризация приложения | - |
| **Nginx** | Веб-сервер для production | Alpine |

##  Быстрый старт

### Предварительные требования

- Node.js 18+ и npm 9+
- или
- Docker

### Установка и запуск в режиме разработки через npm

```bash
# Клонирование репозитория
git clone <repository-url>
cd frontend-DataHive

# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

Приложение будет доступно по адресу: [http://localhost:5173](http://localhost:5173)

###  Сборка Docker

```bash
# Сборка Docker-образа
docker build -f infra/Dockerfile -t datacatalog-frontend .

# Запуск контейнера
docker run -p 8080:80 --name datacatalog-app datacatalog-frontend
```

Приложение будет доступно по адресу: [http://localhost:8080](http://localhost:8080)


##  Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Базовый URL API 
VITE_API_URL=http://{host}/api/v1
```


## Интеграция с Backend API

Frontend ожидает REST API со следующими эндпоинтами:

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/v1/datasets` | Получение списка датасетов |
| `GET` | `/api/v1/datasets/{id}` | Получение детальной информации о датасете |
| `POST` | `/api/v1/search/datasets` | Поиск датасетов по критериям |


## Структура проекта

```
datacatalog-frontend/
├── src/
│   ├── api/           # HTTP-запросы
│   ├── components/    # Переиспользуемые UI-компоненты
│   ├── layout/        # Макеты страниц (обертки)
│   ├── mock/          # Моки данных для разработки
│   └── pages/         # Страницы приложения
├── public/            # Статические файлы
├── infra/             # Инфраструктурные файлы
│   ├── nginx.conf     # Конфигурация Nginx
│   └── Dockerfile     # Docker конфигурация
├── .env.example       # Пример переменных окружения
├── vite.config.ts     # Конфигурация Vite
├── package.json       # Зависимости и скрипты
├── README.md          # Документация проекта
└── LICENSE            # Лицензия
```

##  Скрипты npm

| Скрипт | Назначение |
|--------|------------|
| `npm run dev` | Запуск dev-сервера |
| `npm run build` | Сборка для production |
| `npm run preview` | Просмотр собранного приложения |


##  Развертывание

### На сервере с Docker

1. Скопируйте файлы проекта на сервер
2. Соберите Docker-образ:
   ```bash
   docker build -f infra/Dockerfile -t datacatalog-frontend .
   ```
3. Запустите контейнер:
   ```bash
   docker run -d -p 80:80 --restart unless-stopped --name datacatalog datacatalog-frontend
   ```

##  Roadmap

### 🟢 Ближайшие планы
- [✅] Настройка адреса backend API через `.env`
- [ ] Аутентификация и авторизация пользователей
- [ ] Полноценный пользовательский профиль

### 🟡 Среднесрочные планы
- [ ] Улучшения UI/UX и поддержка тёмной темы
- [ ] Избранные датасеты

### 🔴 Долгосрочные планы
- [ ] Избранные датасеты
- [ ] Экспорт метаданных в CSV/JSON
- [ ] История просмотренных датасетов

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).
